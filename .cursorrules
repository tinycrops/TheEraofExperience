# gemini API tool calling
model = 'gemini-2.0-flash'
GEMINI_API_KEY api keys are in .env.local
we are using the Gemini API (npm i @google/genai) and NOT THE VERTEX API


YOU MUST TEST ALL CHANGES FOR ERRORS BEFORE MOVING ON TO THE NEXT FEATURE IMPLEMENTATION
